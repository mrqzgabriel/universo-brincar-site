# Apenas para teste local: docker compose up --build
# Depois abra http://localhost:8080
services:
  site:
    build: .
    image: universo-brincar:local
    ports:
      - "8080:80"
    restart: unless-stopped
